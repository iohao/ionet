# ${generateTime}
# ${iohaoHome}

## ${mlComment(ActionComment, "## ", "[br]")}[br]
## https://github.com/iohao/ionet
class_name ${ActionName}
extends RefCounted

${imports}

<%for(o in actionMemberCmdDocumentList) {%>
static var _${o.memberName}: int = IoGame.CmdKit.mapping_request(${o.cmdMerge}, "${stringLiteral(o.comment, "double")}")
<%}%>

<%for(o in methodCodeList) {%>
${o}
<%}%>

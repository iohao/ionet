${generateTime}
${iohaoHome}
using System.Linq;
using System.Threading.Tasks;
using System.Collections.Generic;
using Google.Protobuf;
using IoGame.Sdk;

namespace ${namespace}
{
  /// <summary>
  /// ${mlComment(ActionComment, "  /// ")}
  /// </summary>
  /// <remarks>Author: https://github.com/iohao/ionet</remarks>
  public static class ${ActionName}
  {
  <%for(o in actionMemberCmdDocumentList) {%>
    ${publicActionCmdName} static readonly int ${o.memberName} = CmdKit.MappingRequest(${o.cmdMerge}, "${stringLiteral(o.comment, "double")}");
  <%}%>

  <%for(o in methodCodeList) {%>
${o}
  <%}%>
  }
}
